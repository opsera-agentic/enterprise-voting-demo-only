# sperivote11 - Opsera Code-to-Cloud Enterprise

## Architecture
```
┌─────────────────────────────────────────────────────────────────┐
│                    sperivote11 - 3 Microservices                │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────┐        ┌─────────────┐        ┌──────────────┐    │
│  │  Vote   │───────▶│    Redis    │◀───────│   Worker     │    │
│  │ (Python)│        │ ElastiCache │        │   (.NET)     │    │
│  └─────────┘        └─────────────┘        └──────┬───────┘    │
│       │                                           │            │
│       │              ┌─────────────┐              │            │
│       │              │  PostgreSQL │◀─────────────┘            │
│       │              │     RDS     │                           │
│       │              └──────┬──────┘                           │
│       │                     │                                  │
│       └─────────────▶┌──────┴──────┐                           │
│                      │   Result    │                           │
│                      │  (Node.js)  │                           │
│                      └─────────────┘                           │
└─────────────────────────────────────────────────────────────────┘
```

## Endpoints
| Service | URL |
|---------|-----|
| Vote | https://vote-sperivote11-dev.agent.opsera.dev |
| Result | https://result-sperivote11-dev.agent.opsera.dev |

## Touchless Deployment

### 1. Bootstrap (Infrastructure + ArgoCD)
```bash
gh workflow run "00-Bootstrap (sperivote11)" -f action=apply
```

### 2. Build & Deploy
```bash
gh workflow run "CI Build (sperivote11-dev)"
```

### 3. E2E Test
```bash
gh workflow run "E2E Test (sperivote11)" -f cats_votes=7 -f dogs_votes=3
```

## Workflows
| Workflow | Purpose |
|----------|---------|
| `00-Bootstrap (sperivote11)` | Terraform + ArgoCD setup |
| `CI Build (sperivote11-dev)` | Build images, deploy to DEV |
| `E2E Test (sperivote11)` | Voting simulation test |

---
_Generated by Opsera Code-to-Cloud Enterprise v0.905_
