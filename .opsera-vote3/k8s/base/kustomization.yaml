apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Note: namespace is set by overlays, not base

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: vote3

resources:
  - namespace.yaml
  - vote-deployment.yaml
  - vote-service.yaml
  - result-deployment.yaml
  - result-service.yaml
  - worker-deployment.yaml
  - ingress.yaml
# Note: configmap.yaml and serviceaccounts.yaml are in overlays (environment-specific)
