################################################################################
# ArgoCD Application - vote3 Staging Environment (Blue-Green Deployment)
# Generated by Opsera Code-to-Cloud Enterprise v0.905
#
# Architecture:
#   Hub Cluster: argocd-usw2 (runs ArgoCD)
#   Spoke Cluster: opsera-usw2-np (runs workloads)
#
# Deployment Strategy: Blue-Green with preview URLs
# RULE 59: Use destination.name for spoke cluster, NOT destination.server
################################################################################

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vote3-staging
  namespace: argocd
  labels:
    app.kubernetes.io/name: vote3
    app.kubernetes.io/instance: vote3-staging
    app.kubernetes.io/managed-by: argocd
    environment: staging
    tenant: opsera
    deployment-strategy: blue-green
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/opsera-agentic/enterprise-voting-app-t1.git
    targetRevision: main
    path: .opsera-vote3/k8s/overlays/staging

  # RULE 59: Hub-Spoke - Deploy to spoke cluster by NAME
  destination:
    name: opsera-usw2-np
    namespace: vote3-staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    - group: argoproj.io
      kind: Rollout
      jsonPointers:
        - /spec/replicas

  revisionHistoryLimit: 10
