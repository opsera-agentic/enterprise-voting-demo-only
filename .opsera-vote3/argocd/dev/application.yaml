################################################################################
# ArgoCD Application - vote3 DEV Environment
# Generated by Opsera Code-to-Cloud Enterprise v0.905
#
# Architecture:
#   Hub Cluster: argocd-usw2 (runs ArgoCD)
#   Spoke Cluster: opsera-usw2-np (runs workloads)
#
# RULE 59: Use destination.name for spoke cluster, NOT destination.server
################################################################################

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vote3-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: vote3
    app.kubernetes.io/instance: vote3-dev
    app.kubernetes.io/managed-by: argocd
    environment: dev
    tenant: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/opsera-agentic/enterprise-voting-app-t1.git
    targetRevision: main
    path: .opsera-vote3/k8s/overlays/dev

  # RULE 59: Hub-Spoke - Deploy to spoke cluster by NAME
  destination:
    name: opsera-usw2-np
    namespace: vote3-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas

  revisionHistoryLimit: 10
